# Configuración de la base de datos MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/wishport_db?createDatabaseIfNotExist=true&serverTimezone=Europe/Madrid
spring.datasource.username=root
spring.datasource.password=tu_password_aqui
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Configuración del servidor
server.port=8080

# Clave secreta para JWT (cámbiala en producción)
wishport.jwt.secret=mi_clave_secreta_muy_larga_y_segura_para_firmar_tokens_jwt_debe_tener_al_menos_256_bits

# Configuración de logging
logging.level.com.wishport.backend=DEBUG
logging.level.org.springframework.security=DEBUG
